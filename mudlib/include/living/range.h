#ifndef RANGE_H
#define RANGE_H

// The handler that answers how far apart two things are, and whether that is
// close enough: /lib/handlers/range.c

#define RANGE_HANDLER "/lib/handlers/range.c"

#endif
