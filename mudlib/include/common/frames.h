
#define DEFAULT_FRAME_STYLE "default"
#define DEFAULT_WIDTH 79
#define DEFAULT_HEIGHT 0

// styles are now mappings keyed by name (see /lib/handlers/frames.c
// for the field shape). The legacy STYLE_* positional indexes are
// gone — anything that read them needs to switch to the named keys.
