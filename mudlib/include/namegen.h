
#ifndef NAMEGEN_H
#define NAMEGEN_H

// Game-agnostic name generator (packages/namegen). Learns the shape of a
// per-culture wordlist and produces new names in that style. Call it as:
//   NAMEGEN_OB->generate_name("/games/<game>/.../<culture>.names")
#define NAMEGEN_OB "/packages/namegen/master"

#endif // NAMEGEN_H
