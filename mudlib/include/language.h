
// dummy file, it will never be included, as the include_file
// is masked inside driver.c

// every time a file would try to include this headers file, 
// the driver will look for a specific file in the same directory
// as the caller
